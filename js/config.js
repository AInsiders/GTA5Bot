/**
 * Optional config for GitHub Pages / production.
 * Set your stats API URL here when deployed (e.g. Render, Railway).
 * Leave as-is for localhost (auto-uses http://127.0.0.1:8765).
 */
// window.__NEON_STATS_API_URL__ = 'https://your-stats-api.fly.dev';
